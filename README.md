# Email Extractor 📧

## Overview
This Python script extracts all email addresses from a `.txt` file and saves them into another file.

---

## Features
- Uses **regex** to detect valid email addresses.
- Saves results into a new file (`output.txt`).
- Prints the number of emails found.

---

## How to Use
1. Place your text file (e.g., `input.txt`) in the project folder.
2. Run the script:
   ```bash
   python codealpha3.py
